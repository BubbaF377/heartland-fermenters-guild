{
  "name": "heartland-fermenters-guild",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "dev:emulators": "firebase emulators:exec --project demo-heartland-fermenters-guild --only auth,firestore,storage --ui 'PUBLIC_FIREBASE_EMULATORS=true astro dev'",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest",
    "test:e2e": "firebase emulators:exec --project demo-heartland-fermenters-guild --only auth,firestore,storage 'playwright test'",
    "test:e2e:ui": "firebase emulators:exec --project demo-heartland-fermenters-guild --only auth,firestore,storage 'playwright test --ui'",
    "test": "npm run test:unit && npm run test:rules && npm run test:e2e",
    "test:rules": "firebase emulators:exec --project demo-heartland-fermenters-guild --only auth,firestore,storage 'vitest run --config vitest.rules.config.js'"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "firebase": "^12.19.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "@playwright/test": "^1.62.1",
    "firebase-tools": "^15.30.2",
    "vitest": "^4.1.11"
  }
}
